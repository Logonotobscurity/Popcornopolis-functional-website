import React from 'react';
import ReactDOM from 'react-dom/client';
import ClipinApp from './clipin/App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ClipinApp />
  </React.StrictMode>
);
